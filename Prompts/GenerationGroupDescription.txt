TASK:
Review the provided Groups and Objects. For Groups missing a "description", generate a comprehensive, snapshot-like summary of their contents. For Groups with an existing description, preserve it unchanged.

INPUT CONTEXT:
{0}
(Contains "groups" array and "objects" array with hierarchy data)

INSTRUCTIONS:

1. PROCESSING LOGIC:
   - Iterate through every Group in the input list.
   - Case A: The Group ALREADY has a non-empty "description".
	 -> ACTION: RETAIN the existing group object exactly as is. Do NOT modify the text.
   - Case B: The Group has a missing, null, or empty "description".
	 -> ACTION: Generate a new description using the "Synthesis Rules" below.
   - RESULT: The output "groups" array must contain ALL groups (both preserved and newly generated).

2. ANALYSIS (For Case B only):
   - Identify the Group's spatial boundaries based on `cell_indices`.
   - Identify ALL objects belonging to this group.
   - Distinguish between:
     - Root Objects: Objects placed directly on the grid cells.
     - Child Objects: Objects attached/linked to other objects (check parent_id or children lists).

3. SYNTHESIS RULES (For Case B only):
   - Goal: Create a self-contained textual representation of the group's current state (layout snapshot).
   - Rule 1 (Completeness): You MUST mention EVERY object contained within the group by its name/type.
   - Rule 2 (Hierarchy): Explicitly describe the relationship between objects. If Object B is linked to Object A, describe it naturally (e.g., "placed on top of," "inside").
   - Rule 3 (Positioning): Describe the relative placement of Root Objects within the group (e.g., "in the center," "in the corner," "adjacent to").
   - Rule 4 (No Redundancy): Do NOT describe the visual appearance, material, or state of the individual objects. Detailed descriptions exist elsewhere; focus strictly on the object type (Name) and its position/hierarchy within the group.

4. CONSTRAINTS:
   - NO Technical Data: Do NOT include numeric IDs, UUIDs, or raw cell indices in the description.
   - NO Flavor Text: Keep it functional.
   - NO Narrative: Do not invent a story. Just describe the static arrangement.

OUTPUT FORMAT:
{{
	"groups": [
		{{
			"description": "EXISTING DESCRIPTION...", // Case A: Preserved
			"cell_indices": ["..."]
		}},
		{{
			"description": "A Table stands in the center. A Candle is placed on the Table. A Chair is located to the left.", // Case B: Generated
			"cell_indices": ["..."]
		}}
	]
}}
